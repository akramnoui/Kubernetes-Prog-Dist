# Ignore node_modules in the frontend (restaurant-search)
restaurant-search/node_modules/

# Ignore build artifacts for frontend (if using React or other front-end frameworks)
restaurant-search/build/
restaurant-search/dist/
restaurant-search/.next/
restaurant-search/.nuxt/
restaurant-search/coverage/

# Ignore environment variable files (for both frontend and backend)
restaurant-search/.env
backend/.env

# Ignore logs and error files for both frontend and backend
restaurant-search/*.log
backend/*.log

# Ignore the backend's node_modules (if applicable)
backend/node_modules/

# Ignore IDE configuration files (for both frontend and backend)
*.idea/
*.vscode/
*.sublime-workspace
*.sublime-project

# Ignore OS-generated files (macOS, Windows, etc.)
.DS_Store
Thumbs.db

# Ignore all temporary files (e.g., for testing, builds)
*.tmp
*.swp
*.bak

# Ignore dependency lock files (optional, depending on your use case)
# restaurant-search/package-lock.json
# backend/package-lock.json

# Ignore test coverage files
restaurant-search/coverage/
backend/coverage/

# Ignore database files (if applicable)
backend/*.sqlite
backend/*.db

# Ignore compiled binary files
backend/**/*.out
backend/**/*.exe
